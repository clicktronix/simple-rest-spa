import * as mainRoutes from './Main/routes';
import * as userRoutes from './Users/routes';
import * as authRoutes from './Auth/routes';

export const routes = {
  mainRoutes, userRoutes, authRoutes,
};
